<span class="wrapper">
	<span class="text"> Javascript</span>
</span>

<style>
	/* Google Fonts */
	@import url(https://fonts.googleapis.com/css?family=Anonymous+Pro);

	.wrapper {
		display: inline-flex;
		background-color: slate.2;
		@media (prefers-color-scheme: dark) {
			background-color: slate.7;
		}
		height: min-content;
		padding: 2xs;
		border-radius: lg;

		break-before: always;
	}

	.text {
		display: inline-block;
		overflow: hidden;

		/* FONT */
		font-family: mono;
		font-size: lg;
		letter-spacing: 2px;
		white-space: nowrap;

		/* ANIMATION */
		animation: typing 2s steps(10, end), blink 900ms step-end infinite;
		border-right: 4px solid;
		border-color: cyan.8;
		@media (prefers-color-scheme: dark) {
			border-color: cyan.3;
		}
	}

	@keyframes typing {
		from {
			width: 0%;
		}
		to {
			width: 100%;
		}
	}

	@keyframes blink {
		from,
		to {
			border-color: transparent;
		}
		50% {
			border-color: unset;
		}
	}
</style>
